---
extends: default

rules:
  braces:
    max-spaces-inside: 1
    level: error
  brackets:
    max-spaces-inside: 1
    level: error
  line-length: disable
  truthy: disable

ignore: |
  .github/stale.yml

yaml-files:
  - 'Ansible/**/*.yaml'
  - 'Ansible/**/*.yml'